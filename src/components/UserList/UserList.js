import './UserList.scss';
import React from 'react';

export const UserList = () => {
        return (
            <div className="UserList">
                <p>UserList works!</p>
            </div>
        );
}